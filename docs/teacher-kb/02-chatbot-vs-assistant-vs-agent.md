# Chatbot vs Assistant vs Agent

## What is the Difference?

### Chatbot

A chatbot generates text responses to your questions. It cannot take real
actions — it can only talk about things. If you ask it to write a file, it
will generate text that looks like a file, but no file is actually created.

### Assistant

An assistant is a chatbot with some built-in capabilities, like searching the
web or looking up information. It can access specific data sources but
generally cannot take actions on your behalf.

### Agent

An agent can plan, use tools, and take real actions. It can read your files,
write code, search the web, run commands, and execute multi-step workflows.
The key difference: an agent's actions are real and verifiable, not just text.

## Where Squidley Fits

Squidley is designed to be an agent that teaches you how agents work. In Local
Mode, she currently acts like a chatbot (generating text only). As Cloud Mode
and tools are built, she will gain agent capabilities — but she teaches you
every concept along the way.

## The Key Distinction

When a chatbot says "I wrote the file," that is just text — no file was
created. When an agent writes a file, there is a receipt proving what happened.
Squidley's honesty system catches cases where the model claims to have done
something it did not.

## Check Your Understanding

- What can an agent do that a chatbot cannot?
- How can you tell if an action was real vs just generated text?
