/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remote cockpit — no need for strict React mode warnings cluttering the console
  reactStrictMode: false,
};

export default nextConfig;
